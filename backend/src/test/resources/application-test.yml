spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mnfmanager_test
    username: mnf
    password: mnf_local_password
  jpa:
    hibernate:
      ddl-auto: create-drop
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  sql:
    init:
      mode: never