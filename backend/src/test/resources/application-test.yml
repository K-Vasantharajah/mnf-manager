spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mnfmanager_test
    username: mnf
    password: mnf_local_password
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never